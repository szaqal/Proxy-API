apiVersion: v1
kind: Service
metadata:
  name: {{ include "proxy-api.fullname" . }}
  labels:
    {{- include "proxy-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "proxy-api.selectorLabels" . | nindent 4 }}
